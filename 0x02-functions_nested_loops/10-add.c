#include"main.h"
/**
 * add - adds two integers and returns the result
 * @i: first integer for addition
 * @j: second integer for addition
 * Return: the result of the summation
 */
int add(int i, int j)
{
	return (i + j);
}
