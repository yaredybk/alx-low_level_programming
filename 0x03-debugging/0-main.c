#include"main.h"
/**
 * main - test the function "positive_or_negative" is correct when tested at 0
 * Return: always 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
