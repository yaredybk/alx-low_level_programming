#include"main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: input number to be calculated
 * Return: the absolute value of the given number
 */
int _abs(int i)
{
	return (abs(i));
}
