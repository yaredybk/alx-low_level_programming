#include"main.h"

/**
 * _strlen - returns the length of a string
 * @s: input string
 *
 * Return: length of the input string
 */
int _strlen(char *s)
{
	char *ss;
	int i;

	ss = s;
	i = strlen(ss);
	return (i);
}
