#include <stdio.h>
/**
 * main - print alphabets
 * Return: always 0
 */
int main(void)
{
for (char  i = 'a'; i <= 'z'; i++)
putchar(i);
for (char  i = 'A'; i <= 'Z'; i++)
putchar(i);
putchar(n);
return (0);
}
